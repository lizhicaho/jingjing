# 贡献指南

感谢你愿意改进“我想静静”。

## 提交前检查

1. 保持项目无框架、低资源、可离线游玩的原则。
2. 不引入广告、追踪代码、账号体系或未经说明的第三方网络请求。
3. 不提交密钥、个人照片、浏览器导出的本地数据或生产环境配置。
4. 修改 JavaScript 后执行：

   ```bash
   node --check game.js
   ```

5. 在桌面端与手机窄屏下手动验证：点击、触摸、空格键、回车键、静音与刷新后的本地进度。

## Issue 与 Pull Request

- Bug 请说明浏览器、系统、复现步骤和预期行为。
- 功能建议请说明它如何保持“轻量、安静、无干扰”。
- Pull Request 请尽量聚焦单一主题，并附上验证结果。

## 发布与部署规则

- 默认只在本地完成修改和验证，不自动提交 Git、推送远程仓库或部署服务器。
- 只有仓库维护者明确提出“提交”“推送”或“部署”指令后，才执行对应的外部操作。
- 发布前应说明本次变更范围、验证结果及目标环境；禁止提交密码、私钥、证书或真实统计数据库文件。

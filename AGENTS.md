# AGENTS.md

## 项目定位

“我想静静”是一个轻量、移动端优先的静态敲木鱼小游戏。

- 前端：原生 HTML、CSS、JavaScript，无构建工具和前端框架。
- 线上统计：同源 PHP + SQLite 接口；本地直接打开 HTML 时统计保持为 0。
- 生产试玩地址：`https://jingjing.chaogeapi.online`。

## 目录说明

```text
index.html          页面结构
style.css           视觉、响应式布局、动画
game.js             玩法、本地进度、音效、统计接口调用
stats-config.js     统计接口公开地址配置
assets/             可公开分发的本地音效素材
server/stats.php    PHP + SQLite 匿名统计接口
deploy/             Nginx 部署配置示例
```

## 本地开发与检查

无需安装依赖。可直接打开 `index.html`，或启动简单静态服务器：

```bash
python3 -m http.server 8080
```

修改 JavaScript 后必须执行：

```bash
node --check game.js
```

修改 PHP 统计接口后必须执行：

```bash
php -l server/stats.php
```

修改后至少手动验证：

- 鼠标、触摸、空格键、回车键均可敲击；
- 移动端连续点击不会触发双击缩放；
- 音效开关有效；
- 108 声完成仪式、图片玩法与本地进度正常；
- 道场统计接口不可用时不影响本地玩法。

## 代码约束

- 保持零框架、低资源、无广告、无埋点、无默认第三方网络请求的风格。
- CSS 动画优先使用 `transform` 和 `opacity`；移动端优先确保木鱼点击区域清晰、足够大。
- 新增静态资源前确认许可；第三方素材必须在 `NOTICE` 或 README 中署名。
- 统计接口只允许接收匿名设备 ID 与动作类型，严禁上传照片、烦恼签或个人身份信息。

## 安全要求

- 绝不提交密码、SSH 私钥、SSL 证书私钥、Token、数据库文件、`.env` 文件或服务器备份。
- Git 提交前检查是否误加入 `.key`、`.pem`、`.crt`、`.pfx`、`.sqlite`、`.db` 等敏感文件。
- 证书私钥与统计数据库只能保存在服务器受限目录，不能放进网站根目录或仓库。

## 提交与部署规则

- 默认只在本地修改和验证，**不得自动提交 Git、推送远程仓库或部署服务器**。
- 只有维护者明确提出“提交”“推送”或“部署”时，才执行相应的外部操作。
- 执行提交前，先说明变更范围并完成敏感内容检查；执行部署前，先说明目标环境和影响范围。
- 不使用强制推送、覆盖远端历史或破坏性 Git 命令，除非维护者明确授权。
